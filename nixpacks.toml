[providers]
python = true
node = false

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "cd backend && python -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "cd backend && . /opt/venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
